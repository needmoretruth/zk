[package]
name = "sys-circle-stark"
description = "Circle STARKs over Mersenne31 with Plonky3's circle commitment, proving the museum's seven statements without hiding."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
# Built from source only; nothing here is published to crates.io (see the README).
publish = false

[dependencies]
zk-core = { path = "../zk-core" }
zk-circuit = { path = "../zk-circuit" }
zk-examples = { path = "../zk-examples" }
p3-air = "0.7.0"
p3-challenger = "0.7.0"
p3-circle = "0.7.0"
p3-commit = "0.7.0"
p3-field = "0.7.0"
p3-fri = "0.7.0"
p3-keccak = "0.7.0"
p3-matrix = "0.7.0"
p3-merkle-tree = "0.7.0"
p3-mersenne-31 = "0.7.0"
p3-symmetric = "0.7.0"
p3-uni-stark = "0.7.0"
postcard = { version = "1.1.3", default-features = false, features = ["alloc"] }

[lints]
workspace = true
