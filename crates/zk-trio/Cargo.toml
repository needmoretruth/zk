[package]
name = "zk-trio"
description = "Trio, the museum's own proof: three imaginary friends compute the circuit on shares, a dealer hands out multiplication cards, and five cards keep them honest with nothing but a hash."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
zk-core = { path = "../zk-core" }
zk-circuit = { path = "../zk-circuit" }
zk-examples = { path = "../zk-examples" }
p3-field = "0.7.0"
p3-goldilocks = "0.7.0"
sha2 = { version = "0.11", default-features = false }
getrandom = "0.4"

[lints]
workspace = true
